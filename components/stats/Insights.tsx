import { DivideStats, Question } from '@/lib/types';
export function Insights({ stats, question }: { stats: DivideStats; question: Question }) { const mostCertainTitle = stats.confidenceLiftPercentage >= 0 ? question.optionA.title : question.optionB.title; return <div className="grid gap-px border border-zinc-800 bg-zinc-800 sm:grid-cols-2"><div className="bg-zinc-950 p-5"><p className="font-mono text-[10px] tracking-[.18em] text-amber-300">MOST CERTAIN</p><p className="mt-4 text-sm leading-6 text-zinc-300">{Math.abs(stats.confidenceLiftPercentage).toFixed(0)}% higher confidence among {mostCertainTitle} voters.</p></div><div className="bg-zinc-950 p-5"><p className="font-mono text-[10px] tracking-[.18em] text-indigo-300">FASTEST SPLIT</p><p className="mt-4 text-sm leading-6 text-zinc-300">{stats.fastestSplitPercentage.toFixed(0)}% of sub-1-second decisions went to {question.optionA.title}.</p></div></div>; }
