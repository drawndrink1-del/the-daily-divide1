import { DivideStats, Question } from '@/lib/types';
export function ConfidenceChart({ stats, question }: { stats: DivideStats; question: Question }) { const max = 10; const options = [{ key: question.optionA.key, label: question.optionA.title, color: question.optionA.color }, { key: question.optionB.key, label: question.optionB.title, color: question.optionB.color }]; return <div className="border border-zinc-800 bg-zinc-900/30 p-5"><div className="flex items-start justify-between"><div><p className="font-mono text-[10px] tracking-[.18em] text-zinc-600">CONFIDENCE BY CHOICE</p><p className="mt-2 text-sm text-zinc-300">Certainty has a shape</p></div><span className="font-mono text-[10px] text-zinc-600">/10 AVG</span></div><div className="mt-8 space-y-5">{options.map(option => { const score = stats.choices[option.key].averageConfidence; return <div key={option.key}><div className="mb-2 flex justify-between font-mono text-[10px] text-zinc-500"><span style={{ color: option.color }}>{option.label}</span><span>{score.toFixed(1)}</span></div><div className="h-2 bg-zinc-800"><div className="h-full transition-all" style={{ width: `${score / max * 100}%`, backgroundColor: option.color }} /></div></div>; })}</div></div>; }
