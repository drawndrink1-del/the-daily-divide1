import { DivideStats } from '@/lib/types';
export function SpeedChart({ stats }: { stats: DivideStats }) { const max = Math.max(...stats.speedBuckets.map(bucket => bucket.count), 1); return <div className="border border-zinc-800 bg-zinc-900/30 p-5"><div className="flex items-start justify-between"><div><p className="font-mono text-[10px] tracking-[.18em] text-zinc-600">DECISION SPEED</p><p className="mt-2 text-sm text-zinc-300">Impulse to deliberation</p></div><span className="font-mono text-[10px] text-zinc-600">COUNT</span></div><div className="mt-8 flex h-36 items-end gap-2 border-b border-zinc-800">{stats.speedBuckets.map(bucket => <div key={bucket.label} className="group flex h-full flex-1 flex-col justify-end"><div className="relative w-full bg-zinc-700 transition-colors group-hover:bg-amber-400" style={{ height: `${bucket.count / max * 100}%` }}><span className="absolute -top-5 left-1/2 -translate-x-1/2 font-mono text-[9px] text-zinc-500">{bucket.count}</span></div><span className="mt-3 text-center font-mono text-[9px] text-zinc-600">{bucket.label}</span></div>)}</div></div>; }
