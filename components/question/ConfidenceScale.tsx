'use client';
import { motion } from 'framer-motion';
import { Button } from '@/components/ui/Button';

export function ConfidenceScale({ value, onChange, onSubmit, disabled = false }: { value: number; onChange: (value: number) => void; onSubmit: () => void; disabled?: boolean }) { return <div className="border-t border-zinc-800 pt-8"><div className="flex items-end justify-between gap-5"><div><p className="font-mono text-[10px] tracking-[.22em] text-amber-300">SECOND DIMENSION</p><h2 className="mt-3 text-2xl font-medium tracking-tight text-zinc-100">How confident are you?</h2></div><span className="font-mono text-4xl text-zinc-100">{value}<span className="text-base text-zinc-600">/10</span></span></div><div className="mt-8 flex gap-1.5 sm:gap-2">{Array.from({ length: 10 }, (_, index) => { const level = index + 1; return <motion.button disabled={disabled} whileTap={{ scale: .9 }} type="button" key={level} onClick={() => onChange(level)} aria-label={`Confidence ${level} out of 10`} className={`h-11 flex-1 border transition ${level <= value ? 'border-amber-300 bg-amber-300 text-zinc-950' : 'border-zinc-800 bg-zinc-900 text-zinc-600 hover:border-zinc-600'}`}><span className="font-mono text-xs">{level}</span></motion.button>; })}</div><div className="mt-3 flex justify-between text-[10px] uppercase tracking-[.12em] text-zinc-600"><span>Pure hesitation</span><span>Absolute certainty</span></div><div className="mt-8 flex justify-end"><Button disabled={disabled} onClick={onSubmit}>Reveal the divide</Button></div></div>; }
