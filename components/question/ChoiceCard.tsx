'use client';
import { motion } from 'framer-motion';
import { ArrowUpRight } from 'lucide-react';
import { Question } from '@/lib/types';

export function ChoiceCard({ option, onSelect, disabled }: { option: Question['optionA']; onSelect: () => void; disabled?: boolean }) { return <motion.button type="button" disabled={disabled} whileHover={{ y: -5, scale: 1.015 }} whileTap={{ scale: .985 }} onClick={onSelect} className="group relative min-h-[240px] overflow-hidden border border-zinc-800 bg-zinc-900/45 p-6 text-left transition-colors duration-300 hover:border-zinc-600 disabled:pointer-events-none sm:p-8" style={{ '--choice-color': option.color } as React.CSSProperties}><div className="absolute -right-20 -top-20 h-48 w-48 rounded-full opacity-0 blur-3xl transition-opacity duration-500 group-hover:opacity-20" style={{ backgroundColor: option.color }} /><div className="relative flex h-full flex-col justify-between"><div className="flex items-start justify-between"><span className="font-mono text-[10px] tracking-[.25em]" style={{ color: option.color }}>{option.eyebrow}</span><ArrowUpRight className="h-5 w-5 text-zinc-700 transition group-hover:text-zinc-200" /></div><div><h2 className="font-mono text-2xl font-bold tracking-[-.04em] text-zinc-100 sm:text-3xl">{option.title}</h2><p className="mt-3 max-w-xs text-sm leading-6 text-zinc-500">{option.detail}</p></div></div></motion.button>; }
