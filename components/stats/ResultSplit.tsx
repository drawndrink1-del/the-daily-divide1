'use client';
import { motion } from 'framer-motion';
import { DivideStats, Question } from '@/lib/types';
export function ResultSplit({ stats, question }: { stats: DivideStats; question: Question }) { const optionAPercentage = stats.choices.A.percentage; const optionBPercentage = stats.choices.B.percentage; return <div className="border-y border-zinc-800 py-8"><div className="mb-5 flex items-end justify-between"><div><p className="font-mono text-[10px] tracking-[.2em] text-zinc-600">THE SPLIT</p><p className="mt-2 text-sm text-zinc-400">What the crowd chose</p></div><p className="font-mono text-xs text-zinc-600">{stats.total.toLocaleString()} total decisions</p></div><div className="relative h-14 overflow-hidden bg-indigo-400/80"><motion.div initial={{ width: 0 }} animate={{ width: `${optionAPercentage}%` }} transition={{ duration: 1, ease: 'easeOut' }} className="absolute inset-y-0 left-0 bg-amber-400" /><div className="absolute inset-y-0 flex w-full items-center justify-between px-4 font-mono text-sm font-bold text-zinc-950"><span>{optionAPercentage.toFixed(1)}% · {question.optionA.title}</span><span>{optionBPercentage.toFixed(1)}% · {question.optionB.title}</span></div><motion.div initial={{ left: 0 }} animate={{ left: `${optionAPercentage}%` }} transition={{ duration: 1, ease: 'easeOut' }} className="absolute top-0 h-full w-px bg-white/80 shadow-[0_0_14px_white]" /></div></div>; }
