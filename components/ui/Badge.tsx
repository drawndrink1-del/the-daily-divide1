export function Badge({ children, tone = 'neutral' }: { children: React.ReactNode; tone?: 'neutral' | 'live' }) { return <span className={`inline-flex items-center gap-2 rounded-full border px-3 py-1.5 text-[10px] uppercase tracking-[.16em] ${tone === 'live' ? 'border-amber-400/30 bg-amber-400/5 text-amber-300' : 'border-zinc-800 text-zinc-500'}`}><span className={`h-1.5 w-1.5 rounded-full ${tone === 'live' ? 'bg-amber-400 shadow-[0_0_8px_#f59e0b]' : 'bg-zinc-600'}`} />{children}</span>; }
