'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { Archive, Info, LockKeyhole, Sparkles } from 'lucide-react';
import { getDayNumber } from '@/lib/questions';

export function Navigation({ dayNumber = getDayNumber(new Date()) }: { dayNumber?: number }) { const pathname = usePathname(); return <header className="relative z-10 border-b border-zinc-800/80 bg-zinc-950/90 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/" className="group flex items-center gap-3"><span className="flex h-7 w-7 items-center justify-center rounded-sm border border-zinc-600 text-[10px] font-bold transition group-hover:border-amber-400 group-hover:text-amber-400">DD</span><span className="hidden text-[11px] font-semibold tracking-[.2em] text-zinc-200 sm:block">THE DAILY DIVIDE</span></Link><nav className="flex items-center gap-5 text-[11px] font-medium tracking-wide text-zinc-500 sm:gap-7"><Link className={pathname === '/' ? 'text-zinc-100' : 'hover:text-zinc-200'} href="/">Today&apos;s Divide</Link><Link className={pathname === '/archive' ? 'text-zinc-100' : 'hover:text-zinc-200'} href="/archive"><Archive className="mr-1 inline h-3.5 w-3.5" />Archive</Link><Link className={pathname === '/about' ? 'text-zinc-100' : 'hover:text-zinc-200'} href="/about"><Info className="mr-1 inline h-3.5 w-3.5" />About</Link></nav><div className="hidden items-center gap-2 rounded-full border border-zinc-800 px-3 py-1.5 text-[10px] text-zinc-500 lg:flex"><span className="h-1.5 w-1.5 rounded-full bg-amber-400 shadow-[0_0_8px_#f59e0b]" /> LIVE · DAY {dayNumber} <Sparkles className="ml-1 h-3 w-3 text-zinc-600" /></div></div></header>; }
export function Footer() { return <footer className="border-t border-zinc-800 px-5 py-8 text-xs text-zinc-600"><div className="mx-auto flex max-w-7xl items-center justify-between"><span>THE DAILY DIVIDE / 2026</span><div className="flex items-center gap-4"><Link href="/privacy" className="flex items-center gap-1 hover:text-zinc-300"><LockKeyhole className="h-3 w-3" /> Privacy</Link><Link href="/terms" className="hover:text-zinc-300">Terms</Link></div></div></footer>; }
